/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment to enable static export for GitHub Pages deployment.
  // output: 'export',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
